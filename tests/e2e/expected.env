# Expected outcome of smoke.sh against the plan-neutral fixture.
#
# Changing these numbers means the ACTION'S BEHAVIOUR changed. That is not
# necessarily wrong, but it must be justified in the plan that causes it — see
# README.md. A base-image change must never move them.
EXPECT_TOTAL=3
EXPECT_SUCCESS=2
EXPECT_FAILED=0
EXPECT_SKIPPED=1
EXPECT_EXIT=0
